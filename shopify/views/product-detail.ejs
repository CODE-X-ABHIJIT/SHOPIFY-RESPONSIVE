<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Product Detail</title>
</head>

<body>

    <%- include('./partials/header') %>

    <div class="container mx-auto px-10 py-20">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
            <div class="flex items-center justify-center bg-[<%= product.bgcolor %>] h-96">
                <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="<%= product.name %>" class="h-[20rem]">
            </div>
            <div>
                <h2 class="text-2xl font-bold mb-4"><%= product.name %></h2>

                <!-- Price Breakdown -->
                <h3 class="text-xl font-semibold mb-4">Price Breakdown</h3>
                <div class="space-y-2">
                    <div class="flex justify-between">
                        <span>Total MRP:</span>
                        <span>₹ <%= product.price %></span>
                    </div>
                    <div class="flex justify-between">
                        <span>Discount:</span>
                        <span>- ₹ <%= product.discount %></span>
                    </div>
                    <div class="flex justify-between">
                        <span>Platform Fee:</span>
                        <span>₹ 20</span>
                    </div>
                    <div class="flex justify-between">
                        <span>Shipping Fee:</span>
                        <span>FREE</span>
                    </div>
                </div>
                
                <div class="w-full h-[1px] bg-gray-300 my-4"></div>
                
                <div class="flex justify-between text-lg font-bold">
                    <span>Total Amount:</span>
                    <span>₹ <%= product.price - product.discount + 20 %></span>
                </div>

                <p class="mt-6">Description: <%= product.description %></p>

                <a href="/users/addtocart/<%= product._id %>" class="inline-block mt-6 px-4 py-2 bg-blue-500 text-white rounded">Add to Cart</a>
            </div>
        </div>
    </div>

    <%- include('./partials/footer') %>
</body>

</html>
