<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>SCATCH</title>
</head>

<body class="bg-gray-100">

    <!-- <%- include('./partials/header') %> -->

    <% if(error.length > 0){ %>
        <div class="absolute top-5 left-1/2 transform -translate-x-1/2 p-3 rounded-md bg-red-500">
            <span class="inline-block mt-1 mb-1 text-white">
                <%= error %>
            </span>
        </div>
    <% } %>
    <% if(success.length > 0) { %>
        <div class="absolute top-5 left-1/2 transform -translate-x-1/2 p-3 rounded-md bg-blue-500">
            <span class="inline-block text-white"><%= success %></span>
        </div>
    <% } %>
    <div class="pl-3 text-xl pt-3">
        <h3 class="text-xl pt-3">Shopify</h3>
    </div>
    <div class="flex flex-col md:flex-row items-center justify-center min-h-screen px-5 md:px-20">
        <div class="w-full md:w-1/2 flex items-center justify-center mb-10 md:mb-0">
            <div class="w-full md:px-32">
                <h3 class="text-4xl">welcome to <span class="text-blue-400 font-semibold">Shopify</span></h3>
                <h4 class="text-2xl mb-5">Create your account</h4>
                <form autocomplete="off" action="/users/register" method="post">
                    <input
                        class="bg-zinc-100 block w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="text" placeholder="Full Name" name="fullname">
                    <input
                        class="bg-zinc-100 block w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="email" placeholder="Email" name="email">
                    <input
                        class="bg-zinc-100 block w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="password" placeholder="Password" name="password">
                    <input class="px-5 rounded-full py-3 mt-2 bg-blue-500 text-white w-full md:w-auto" type="submit"
                        value="Create My Account">
                </form>
            </div>
        </div>
        <div class="w-full md:w-1/2 flex items-center justify-center">
            <div class="w-full md:px-32">
                <h4 class="text-2xl capitalize mb-5">login your account</h4>
                <form autocomplete="off" action="/users/login" method="post">
                    <input
                        class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="email" placeholder="Email" name="email">
                    <input
                        class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="password" placeholder="Password" name="password">
                    <input class="px-5 block rounded-full py-3 mt-2 bg-blue-500 text-white w-full md:w-auto" type="submit"
                        value="Login">
                </form>
                <a href="/users/forgot-password" class="block mt-4 text-blue-500 hover:underline">Forgot Password?</a>
            </div>
        </div>
    </div>

    <%- include('./partials/footer') %>

</body>

</html>
